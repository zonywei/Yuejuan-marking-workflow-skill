interface:
  display_name: "Yuejuan Marking Workflow"
  short_description: "Zhixue and web grading workflow."
  default_prompt: "Use $zhixue-marking-workflow for Zhixue or similar web grading tasks: rubric setup, batch paper caching/scoring, submit or recommit, ledger recovery, reconciliation, and teacher reports."

policy:
  allow_implicit_invocation: true

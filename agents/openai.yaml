interface:
  display_name: "Yuejuan Marking Workflow"
  short_description: "AI draft scoring workflow for teachers."
  default_prompt: "Use $zhixue-marking-workflow to prepare rubrics, run AI draft scoring, review risky cases, reconcile results, or report a subjective-question grading workflow."

policy:
  allow_implicit_invocation: true

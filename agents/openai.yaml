interface:
  display_name: "Yuejuan Marking Workflow"
  short_description: "Teacher-focused grading workflow."
  default_prompt: "Use $zhixue-marking-workflow to help a teacher prepare, run, recover, or report a subjective-question grading workflow."

policy:
  allow_implicit_invocation: true
